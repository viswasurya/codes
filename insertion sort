def insertion_sort(arr):
    for i in range(1,len(arr)):
        j = i 
        while arr[j-1] > arr[j] and j > 0 :
            arr[j-1],arr[j] = arr[j],arr[j-1]
            j-=1 
            
arr = [2,8,9,6,1,5]
insertion_sort(arr)
print(arr)

""" insertion sort divides the given array into sorted and unsorted,
    first element is sorted and the then it compares the elements in the un sorted array with the sorted one and swaps the elements"""
